spring:
  profiles:
    active: ${SPRING_PROFILTES_ACTIVE:local}

  flyway:
    enabled: true
    baseline-on-migrate: true

  firebase:
    service-account:
      path: "firebase/service-account.json"